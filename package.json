{
  "name": "vkomari-cf",
  "version": "2.0.0",
  "description": "Virtual probe node — dual-panel data simulator for Komari & CF-VPS-Monitor",
  "type": "module",
  "scripts": {
    "build": "echo Build-ready: static assets in public/",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute vkomari-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
